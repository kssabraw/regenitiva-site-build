<section id="availability" class="training-section">
  <div class="training">
  <div class="training-left">
    <div class="training-eyebrow">Training</div>
    <h2>Want your clinic trained on this treatment?</h2>
    <p class="training-body">Training is delivered in person by board-certified physicians in pain medicine and musculoskeletal ultrasound — to your whole team at once, from providers to front and back-office staff. We schedule around your practice, so there are no closed clinic days and no one learning it secondhand.</p>
    <p class="training-body-last">Your team's first cases are proctored live, and we stay involved with ongoing training as your program grows.</p>
  </div>
  <div class="training-right">
    <h3 class="training-subhead">Available across the United States.</h3>
    <p class="training-sub-body">We work with practices throughout the U.S. Compliance requirements, licensing, and billing vary by state, so we build your program around the one you practice in — wherever that is.</p>
    <p class="training-catch-all">Ready to bring it to your practice? <a href="/contact">Schedule a call</a> and we'll tailor the program to your state.</p>
  </div>
  </div>
</section>

<style>
  .training-section {
    background: var(--rg-bg-subtle);
  }

  .training {
    padding: var(--rg-space-8) var(--rg-space-5);
    max-width: var(--rg-container);
    margin: 0 auto;
  }

  .training-eyebrow {
    font-family: var(--rg-font-display);
    font-size: var(--rg-size-eyebrow);
    font-weight: var(--rg-weight-bold);
    letter-spacing: var(--rg-ls-eyebrow);
    text-transform: uppercase;
    color: var(--rg-text-muted);
    margin-bottom: var(--rg-space-4);
  }

  .training-left h2 {
    margin-bottom: 20px;
  }

  .training-body {
    color: var(--rg-text-secondary);
    margin-bottom: 20px;
  }

  .training-body-last {
    color: var(--rg-text-secondary);
  }

  .training-right {
    border-top: 1px solid var(--rg-stone-400);
    padding-top: 40px;
    margin-top: var(--rg-space-7);
  }

  .training-subhead {
    margin-bottom: var(--rg-space-3);
  }

  .training-sub-body {
    font-size: var(--rg-size-button);
    line-height: var(--rg-lh-small);
    color: var(--rg-text-secondary);
    margin-bottom: var(--rg-space-6);
  }

  .training-catch-all {
    font-size: var(--rg-size-button);
    line-height: var(--rg-lh-small);
    color: var(--rg-text-secondary);
    margin-top: var(--rg-space-6);
  }

  .training-catch-all a {
    color: var(--rg-text-accent);
  }

  @media (min-width: 768px) {
    .training {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 56px;
      align-items: start;
      padding: 140px var(--rg-space-7);
    }

    .training-right {
      border-top: none;
      border-left: 1px solid var(--rg-stone-400);
      padding-top: 0;
      padding-left: 56px;
      margin-top: 0;
    }
  }
</style>
