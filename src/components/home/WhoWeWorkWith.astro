<section id="who-we-work-with" class="who">
  <div class="who-photo">
    <img src="/images/who-we-work-with-photo.webp" alt="Physiatrist consulting with a patient" />
  </div>
  <div class="who-text">
    <h2>Built for musculoskeletal physicians.</h2>
    <p class="who-intro">We work with physicians who already treat pain and injury every day, and who want a better set of tools for the patients in front of them.</p>
    <div class="who-list">
      <p><strong>Physiatrists</strong> expanding non-surgical treatment options</p>
      <p><strong>Sports medicine physicians</strong> treating joint, tendon, and soft tissue injury</p>
      <p><strong>Pain management physicians</strong> looking beyond injections and medication</p>
      <p><strong>Practices treating motor vehicle accident patients</strong>, where documentation and lien strategy matter as much as the clinical work</p>
      <p><strong>Multi-site groups</strong> standardizing regenerative medicine across locations</p>
    </div>
    <p class="who-closing">If you're seeing patients who are running out of conservative options and aren't ready for surgery, there's a program here for you.</p>
    <a href="/who-we-work-with" class="section-link">See how we support your specialty →</a>
  </div>
</section>

<style>
  .who {
    display: flex;
    flex-direction: column;
    background: var(--rg-bg-subtle);
  }

  .who-photo {
    width: 100%;
    height: 360px;
  }

  .who-photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .who-text {
    padding: var(--rg-space-6) var(--rg-space-5) var(--rg-space-7);
  }

  .who-intro {
    color: var(--rg-text-secondary);
    max-width: 460px;
    margin-bottom: var(--rg-space-7);
  }

  .who-list {
    display: flex;
    flex-direction: column;
    gap: var(--rg-space-4);
    margin-bottom: var(--rg-space-6);
  }

  .who-list p {
    color: var(--rg-text-primary);
  }

  .who-closing {
    color: var(--rg-text-secondary);
    margin-bottom: var(--rg-space-2);
  }

  .section-link {
    font-size: var(--rg-size-button);
    font-weight: var(--rg-weight-medium);
    color: var(--rg-text-accent);
    text-decoration: none;
    display: inline-block;
    margin-top: var(--rg-space-2);
  }

  .section-link:hover {
    color: var(--rg-amber-900);
    text-decoration: underline;
  }

  @media (min-width: 768px) {
    .who {
      position: relative;
      overflow: hidden;
    }

    .who-photo {
      position: absolute;
      top: 0;
      left: 0;
      height: 100%;
      width: 45%;
    }

    .who-text {
      margin-left: 45%;
      padding: 88px max(var(--rg-space-8), calc((100% - var(--rg-container)) / 2 + var(--rg-space-7))) 88px var(--rg-space-8);
    }

    .who-list {
      gap: 18px;
    }
  }
</style>
